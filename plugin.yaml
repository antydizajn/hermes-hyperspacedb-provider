name: hyperspacedb
version: "2.5.2"
license: "MIT"
description: "Fail-closed HyperspaceDB memory provider with ordered mutations, provenance gating, and bounded retrieval."
hooks:
  - on_memory_write
pip_dependencies:
  - "hyperspacedb>=3.1.3,<4"
  - "cryptography>=41.0.0"
